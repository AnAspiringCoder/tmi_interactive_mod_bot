const opts = {
    identity: {
      username: "", // Your bot name here
      password: "", // Your oauth string here (including the 'oauth:' part at the beginning)
    },
    channels: [
      "", // Your channel name
      //"", // The bot can be active on multiple channels at a time. Or, you can switch channels by commenting/uncommenting.
    ]
  };

  module.exports = { opts: opts };
  